// Module augmentations for Auth.js v5 are declared inline in src/auth.ts so
// they're guaranteed to be loaded by the TypeScript build. This file is kept
// as a placeholder; do not add Session/JWT augmentations here unless you also
// verify they're picked up by `next build`.
export {};
